import math

def factorial_list(numbers):
    return list(map(math.factorial, numbers))

nums = list(map(int, input("Enter non-negative integers separated by space: ").split()))
result = factorial_list(nums)

print("Factorials:", result)
